<Application x:Class="Увольнения.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Увольнения"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <ControlTemplate x:Key="UvalTable" TargetType="{x:Type ContentControl}">
            <!--<Grid Width="200" VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Label Content="Label" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Grid.Row="0"/>
                <DataGrid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1"/>
            </Grid>-->
            <StackPanel Width="200" VerticalAlignment="Stretch" Orientation="Vertical">
                <Label Content="Label" HorizontalAlignment="Stretch" VerticalAlignment="Top" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="30"/>
                <DataGrid HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </StackPanel>
        </ControlTemplate>

    </Application.Resources>
</Application>
